services:
  kanban:
    build: .
    container_name: linen-kanban
    #ports:
    #  - "8420:5000"
    volumes:
      - ./local_data/boards:/app/data/boards
    restart: unless-stopped
    networks:
      - caddy_proxy

networks:
  caddy_proxy:
    external: true
